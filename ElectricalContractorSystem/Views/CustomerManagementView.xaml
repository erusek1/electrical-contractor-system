<Window x:Class="ElectricalContractorSystem.Views.CustomerManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Customer Management" Height="600" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="Customer Management" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                
                <Button Grid.Column="1" Content="Add Customer" Command="{Binding AddCustomerCommand}" 
                        Style="{StaticResource PrimaryButtonStyle}" Margin="5,0"/>
                <Button Grid.Column="2" Content="Import" Command="{Binding ImportCustomersCommand}" 
                        Style="{StaticResource SecondaryButtonStyle}" Margin="5,0"/>
                <Button Grid.Column="3" Content="Export" Command="{Binding ExportCustomersCommand}" 
                        Style="{StaticResource SecondaryButtonStyle}" Margin="5,0"/>
            </Grid>
        </Border>
        
        <!-- Search Bar -->
        <Border Grid.Row="0" Background="#ECF0F1" Padding="10" Margin="0,60,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         VerticalAlignment="Center" Padding="5"/>
                <Button Grid.Column="2" Content="Clear" Command="{Binding ClearSearchCommand}" 
                        Margin="10,0,0,0" Padding="10,5"/>
            </Grid>
        </Border>
        
        <!-- Customer Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredCustomers}" 
                  SelectedItem="{Binding SelectedCustomer}"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  Margin="10,90,10,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CustomerId}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="200"/>
                <DataGridTextColumn Header="City" Binding="{Binding City}" Width="100"/>
                <DataGridTextColumn Header="State" Binding="{Binding State}" Width="50"/>
                <DataGridTextColumn Header="Zip" Binding="{Binding Zip}" Width="80"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Save" Command="{Binding DataContext.SaveCustomerCommand, 
                                        RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource SmallButtonStyle}" Margin="2"/>
                                <Button Content="Delete" Command="{Binding DataContext.DeleteCustomerCommand, 
                                        RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource DeleteButtonStyle}" Margin="2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock>
                    <TextBlock.Text>
                        <MultiBinding StringFormat="Total Customers: {0}">
                            <Binding Path="Customers.Count"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>