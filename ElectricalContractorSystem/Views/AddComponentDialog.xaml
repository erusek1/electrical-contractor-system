<Window x:Class="ElectricalContractorSystem.Views.AddComponentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Add Component" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Instructions -->
        <TextBlock Grid.Row="0" 
                   Text="Specify the quantity and any notes for this component."
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>
        
        <!-- Quantity -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Quantity:*" FontWeight="SemiBold"/>
            <TextBox Name="QuantityTextBox" Text="{Binding Quantity}">
                <TextBox.Text>
                    <Binding Path="Quantity" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <DataErrorValidationRule/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
        </StackPanel>
        
        <!-- Notes -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Notes (Optional):" FontWeight="SemiBold"/>
            <TextBox Name="NotesTextBox" 
                     Text="{Binding Notes}"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Height="80"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="AddButton" Content="Add" Width="80" Margin="0,0,10,0" 
                    IsDefault="True" Click="AddButton_Click"/>
            <Button Name="CancelButton" Content="Cancel" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
