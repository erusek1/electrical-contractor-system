<UserControl x:Class="ElectricalContractorSystem.Views.WeeklyLaborEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ElectricalContractorSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Background="#2196F3" Padding="20">
            <TextBlock Text="Weekly Labor Entry" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White"/>
            <TextBlock Text="Enter employee hours by day, job, and stage" 
                       FontSize="14" 
                       Foreground="White" 
                       Opacity="0.9"/>
        </StackPanel>
        
        <!-- Week Selection -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Content="◀ Previous Week" 
                        Command="{Binding PreviousWeekCommand}"
                        Width="120" Height="30" Margin="0,0,10,0"/>
                
                <TextBlock Grid.Column="1" 
                           Text="{Binding CurrentWeekDisplay}" 
                           FontSize="16" FontWeight="Bold"
                           VerticalAlignment="Center" HorizontalAlignment="Center"/>
                
                <Button Grid.Column="2" Content="Next Week ▶" 
                        Command="{Binding NextWeekCommand}"
                        Width="120" Height="30" Margin="10,0,20,0"/>
                
                <Button Grid.Column="3" Content="Save All Entries" 
                        Command="{Binding SaveAllCommand}"
                        Background="#4CAF50" Foreground="White"
                        Width="120" Height="30"/>
            </Grid>
        </Border>
        
        <!-- Labor Entry Grid -->
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding EmployeeLaborEntries}" Margin="20">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Employee Header -->
                                <Border Grid.Row="0" Background="#2196F3" Padding="15,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Column="0" 
                                                   Text="{Binding EmployeeName}" 
                                                   FontSize="16" FontWeight="Bold" 
                                                   Foreground="White"/>
                                        
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <TextBlock Text="Total Hours: " 
                                                       Foreground="White" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding TotalHours}" 
                                                       FontWeight="Bold" Foreground="White" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="Yellow"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding TotalHours}" Value="40">
                                                                <Setter Property="Foreground" Value="LightGreen"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                
                                <!-- Daily Entry Grid -->
                                <Grid Grid.Row="1" Margin="15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- Day Headers -->
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Employee" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Monday" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Tuesday" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Wednesday" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                                    <TextBlock Grid.Row="0" Grid.Column="4" Text="Thursday" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                                    <TextBlock Grid.Row="0" Grid.Column="5" Text="Friday" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                                    <TextBlock Grid.Row="0" Grid.Column="6" Text="Total" FontWeight="Bold" HorizontalAlignment="Center" Padding="5"/>
                                    
                                    <!-- Employee Name -->
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding EmployeeName}" 
                                               FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    
                                    <!-- Daily Entry Controls -->
                                    <ItemsControl Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="5" 
                                                  ItemsSource="{Binding DailyEntries}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <UniformGrid Columns="5"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Margin="5">
                                                    <!-- Job Selection -->
                                                    <ComboBox ItemsSource="{Binding DataContext.Jobs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              SelectedValue="{Binding JobId}"
                                                              DisplayMemberPath="DisplayName"
                                                              SelectedValuePath="JobId"
                                                              Height="25" Margin="0,2"/>
                                                    
                                                    <!-- Stage Selection -->
                                                    <ComboBox ItemsSource="{Binding DataContext.Stages, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              SelectedValue="{Binding Stage}"
                                                              Height="25" Margin="0,2"/>
                                                    
                                                    <!-- Hours Input -->
                                                    <TextBox Text="{Binding Hours, UpdateSourceTrigger=PropertyChanged}"
                                                             Height="25" Margin="0,2"
                                                             HorizontalContentAlignment="Center"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    
                                    <!-- Total Hours -->
                                    <TextBlock Grid.Row="1" Grid.Column="6" Text="{Binding TotalHours}"
                                               FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#F44336"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TotalHours}" Value="40">
                                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>